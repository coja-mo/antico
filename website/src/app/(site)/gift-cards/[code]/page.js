import GiftCardDetailClient from './GiftCardDetailClient';

export const metadata = {
  title: 'Gift Card | Antico Ristoranté',
  description: 'View your Antico Ristoranté gift card balance, transaction history, and add it to your Apple or Google Wallet.',
};

export default function GiftCardDetailPage() {
  return <GiftCardDetailClient />;
}
