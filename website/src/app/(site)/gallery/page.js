import GalleryClient from './GalleryClient';

export const metadata = {
  title: 'Gallery — The Art of Arturo | Antico Ristoranté',
  description: 'Explore the artistic world of Chef Arturo Comegna — where culinary mastery meets visual art. A curated gallery of original works by the chef behind Antico.',
};

export default function GalleryPage() {
  return <GalleryClient />;
}
